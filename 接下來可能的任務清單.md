# 接下來可能的任務清單

本檔供人類閱讀者與下一個 Codex/session 了解專案後續可能的實作方向。這不是網站功能，也不需要資料庫；正式功能仍以每一輪使用者指定的計畫為準。

## 目前狀態

- `V10.5A 菜單顯示排序`：已由使用者回報 Render 驗證成功。
- 目前進行：`V10.5B 菜單分類與售完/隱藏狀態`。

## 可能實作順序

| 優先順序 | 階段 | 任務 | 預期成果 | 是否需要 migration | 驗證重點 |
| --- | --- | --- | --- | --- | --- |
| 已完成 | V10.5A | 菜單顯示排序 | owner/admin 可調整菜單顯示順序，customer 看到排序後菜單 | 需要 | 已由使用者回報 Render 驗證成功 |
| 1 | V10.5B | 菜單分類與售完/隱藏狀態 | 店家可標記售完、暫時隱藏，並改善分類呈現 | 需要 | 售完不可加入購物車，隱藏品項不出現在公開菜單 |
| 3 | V10.5C | 促銷與折扣基礎 | 建立簡單折扣規則，例如單品折扣或滿額折扣 | 需要 | 訂單總額正確套用折扣，歷史訂單保留當時折扣資訊 |
| 4 | V10.6A | 營運報表 CSV 匯出 | staff/owner/admin 可匯出訂單與營收摘要 | 不一定 | 匯出資料欄位正確，不含不該公開的個資 |
| 5 | V10.6B | 即時推播升級 | 評估從 polling 升級到 WebSocket 或 SSE | 不一定 | 工作台、取餐看板、顧客歷史能即時更新 |
| 6 | V10.7A | 期末展示整理 | 強化展示流程、操作導覽、角色示範與驗證清單 | 不需要 | 老師或觀眾能依流程完整看懂 V10 成果 |

## 本輪以外暫不處理

- 不做完整促銷系統的複雜疊加規則。
- 不做付款金流串接。
- 不做退款金額拆分或部分退款。
- 不做獨立後台 router。
- 不做 WebSocket，除非後續正式進入 V10.6B。

## 給下一個 session 的提醒

- 先讀 `AGENTS.md` 與 `報告.md`。
- 再跑 `git status --short` 確認當前工作區。
- 若要接續本輪，優先看 `V10.5B 菜單分類與售完/隱藏狀態` 相關改動。
- Render 驗證仍由使用者負責；本機只需完成 test/build/tsc/diff check。
## 最新 roadmap 狀態

- `V10.5B 菜單分類與售完/隱藏狀態`：使用者已回報 Render 驗證成功。
- 本輪合併實作：
  - `V10.5C 單品特價與折扣價基礎`
  - `V10.6A 營運報表 CSV 匯出`
- 後續候選：
  - `V10.6B 即時推播升級`：評估 WebSocket/SSE，降低 polling。
  - `V10.7A 期末展示整理與操作導覽`：整理展示腳本、角色帳號流程、API 驗證步驟。
# 最新 roadmap 狀態

- 本輪進行中：`V10.3G 管理介面中文化與收合體驗` + `V10.3E A/B 測試穩定分流最小切片`。
  - 管理/營運區塊收合已加入。
  - V10.3 進階區英文文案已改中文。
  - A/B metadata 已進一步支援 visitor key 穩定分流。
  - 待工具額度恢復後補本機 build/test/tsc/diff 驗證。

- 已完成並由使用者回報 Render 驗證：`V10.5C 單品特價與折扣價基礎`、`V10.6A 營運報表 CSV 匯出`。
- 本輪已完成本機驗證、待 Render 驗證：`V10.3 菜單版本化進階功能`。
  - 分級版本管理：major/minor version metadata
  - 價格敏感度分析：依同一 logical item 的歷史版本與訂單統計銷售表現
  - A/B 測試最小切片：experiment key/variant metadata 與後台彙整
- 後續候選方向：
  - `V10.3E A/B 顧客分流`：若要從 metadata 進一步走到實驗分流，可新增穩定分流規則，但風險較高。
  - `V10.3F 價格敏感度進階分析`：加入日期區間、分類篩選、版本變動前後比較。
  - `V10.5D 促銷系統整合進階`：優惠券、滿額折扣、日期排程，會需要更明確的促銷資料模型。
  - `V10.6B 即時推播升級`：評估 SSE/WebSocket，取代 polling。

---

## 最新狀態：V10.3F A/B 測試曝光與轉換統計

- 本輪已完成本機實作：`V10.3F A/B 測試曝光與轉換統計`。
- 這是老師講義 V10.3「A/B 測試支援」的延伸切片，補上曝光紀錄、轉換率與 experiment detail。
- 新增 migration：`0013_v10_menu_experiment_exposures`。
- 下一次 Render 驗證後，可把此階段標記為線上驗證成功。

## 後續候選階段

| 優先順序 | 階段 | 目標 | 是否需要 migration |
| --- | --- | --- | --- |
| 1 | V10.3H 價格敏感度分析強化 | 依日期區間、版本與特價狀態彙整銷售表現 | 不一定 |
| 2 | V10.3I A/B 實驗結論輔助 | 以描述性統計標示高曝光、低轉換或高營收 variant | 不一定 |
| 3 | V10.5D 促銷規則擴充 | 從單品特價延伸到滿額折扣或簡單優惠碼 | 可能需要 |
| 4 | V10.6B 即時推播升級 | 評估 SSE/WebSocket 取代 polling | 不一定 |
