# 早餐店訂餐系統 V10 實作報告

## 摘要

本專案已完成 V10.1 到 V10.4B，且使用者已回報 Render 線上驗證成功。
本輪新增 `V10.4C 訂單取消流程`，讓訂單生命週期從送出、製作、完成，再補上保守且可展示的取消規則。

## 已完成且已 Render 驗證

### V10.1 基礎版本化

- 菜單品項改為版本化 ID，例如 `001-01`、`001-02`。
- 訂單引用特定菜單版本。
- 舊版菜單不可加入購物車。
- 舊購物車送出時會回 `409`。

### V10.2 菜單管理與版本展示

- 登入後可新增、編輯、下架菜單。
- 新增 `GET /api/menu/:id/history`。
- 菜單卡片顯示 logical id、版本 badge、近期更新提示。
- 圖片 URL 預覽與錯誤提示已修正。

### V10.3A RBAC 基礎

- `user.roles` 欄位已建立。
- `RBAC_ADMIN_EMAILS` 可自動授予初始 admin。
- 菜單管理需要 `owner/admin`。
- 查看所有訂單需要 `staff/chef/owner/admin`。

### V10.3B 角色申請與審核

- 使用者可申請 `staff/chef`。
- admin 可核准或拒絕申請。
- 核准後角色會合併進使用者 roles。

### V10.3C Admin 使用者角色管理

- admin 可查看使用者列表。
- admin 可直接調整使用者 roles。
- `customer` 角色永遠保留。
- admin 不可移除自己的 `admin` role。

### V10.3D RBAC 審計紀錄

- 新增 `role_audit_logs`。
- admin 核准/拒絕角色申請會留下紀錄。
- admin 直接更新 roles 會留下紀錄。
- 前端可查看角色異動紀錄。

### V10.4A 店員/廚房訂單工作台

- 訂單狀態擴充為 `pending | submitted | preparing | ready | completed`。
- 新增 `GET /api/orders/workbench`。
- 新增 `PATCH /api/orders/:id/status`。
- `chef/owner/admin` 可將訂單推進到製作中與等待取餐。
- `staff/owner/admin` 可將 ready 訂單完成取餐。
- 顧客歷史訂單會顯示真實狀態。

### V10.4B 取餐資訊與顧客備註

- 新增 `orders.customer_note`。
- `OrderResponse` 新增 `pickupCode`，格式為 `A-0007`。
- 顧客送出訂單時可填寫 120 字內備註。
- 工作台與我的訂單歷史都顯示取餐編號與備註。

## V10.4C 訂單取消流程

### 本輪新增

- 新增 migration：`drizzle-v10/0005_v10_order_cancellation_info.sql`。
- `Order.status` 新增 `cancelled`。
- `orders` 新增：
  - `cancel_reason text not null default ''`
  - `cancelled_by text null references user(id)`
  - `cancelled_at timestamp with time zone null`
- `Order` / `OrderResponse` 新增 `cancelReason`、`cancelledBy`、`cancelledAt`。
- 新增 `PATCH /api/orders/:id/cancel`。
- 新增 `cancelOrderBodySchema`：`{ reason?: string }`，trim 後最多 120 字。
- PostgreSQL store 與 JSON store 都支援取消資訊。
- 前端歷史訂單與工作台都支援取消操作與取消資訊顯示。

### 取消規則

- customer 只能取消自己的 `submitted` 訂單。
- `staff/owner/admin` 可取消 `submitted/preparing/ready` 訂單。
- `pending/completed/cancelled` 不可取消。
- chef 不負責取消訂單。
- `cancelled` 是終態，不能再推進狀態或再次取消。

### 不做項目

- 不做退款。
- 不做付款狀態。
- 不做重開訂單。
- 不做取消原因 modal。
- 不做 WebSocket 即時推播。
- 不新增獨立取消 audit table。

## API 摘要

### `PATCH /api/orders/:id/cancel`

- 需要登入。
- body 可省略。
- body 可選 `reason`，最多 120 字。
- 找不到訂單回 `404`。
- 權限不足回 `403`。
- 狀態不可取消回 `409`。
- 成功後訂單狀態變為 `cancelled`，並保存取消者、取消時間與取消原因。

### 所有 Order response

- 回傳 `customerNote`。
- 回傳 `pickupCode`。
- 回傳 `cancelReason`。
- 回傳 `cancelledBy`。
- 回傳 `cancelledAt`。

## 本機驗證命令

```bash
bun test
bun run build
bunx tsc --noEmit --skipLibCheck --moduleResolution bundler --module esnext --target esnext --jsx react-jsx --allowImportingTsExtensions backend.ts frontend/src/App.tsx tests/v10-menu-versioning.test.ts tests/v10-rbac.test.ts tests/v10-role-requests.test.ts tests/v10-admin-users.test.ts tests/v10-role-audit-logs.test.ts tests/v10-order-workbench.test.ts tests/v10-order-pickup-info.test.ts tests/v10-order-cancellation.test.ts
git diff --check
```

## Render 驗證清單

V10.4C 部署後請確認：

- migration log 出現 `0005_v10_order_cancellation_info`。
- customer 送出訂單後，可在「我的訂單歷史」取消 `submitted` 訂單。
- customer 無法取消已進入 `preparing/ready/completed` 的訂單。
- staff 可在工作台取消 `submitted/preparing/ready` 訂單。
- chef 看不到取消操作，或呼叫取消 API 回 `403`。
- 取消後訂單顯示「已取消」，且不再出現下一步製作/完成按鈕。

## 後續可做

### 營運流程

- 訂單付款。
- 訂單退款。
- 重開訂單。
- 叫號或取餐通知。
- WebSocket 即時推播。

### 後台管理

- audit log 搜尋與匯出。
- admin 使用者搜尋/分頁。
- 使用者角色變更備註強化。

### 菜單與行銷

- display order。
- major/minor version。
- 版本比較 UI。
- 促銷系統。
- A/B testing。

## V10.4D 到店付款狀態

### 狀態更新

- `V10.4C 訂單取消流程` 已由使用者回報 Render 驗證成功。
- 「購物車舊版/已下架品項仍可看見並清空」遺留問題修正已由使用者回報 Render 驗證成功。
- 本輪實作 `V10.4D 到店付款狀態`，讓 `completed` 明確代表「已取餐且已付款」。

### 本輪實作內容

- 新增 migration：`drizzle-v10/0006_v10_order_payment_status.sql`。
- `orders` 新增 `payment_status`、`paid_by`、`paid_at`。
- `Order` / `OrderResponse` 新增 `paymentStatus`、`paidBy`、`paidAt`。
- 新訂單與已送出訂單預設為 `unpaid`。
- 廚房角色推進 `submitted -> preparing -> ready` 不會改付款狀態。
- 櫃台角色執行 `ready -> completed` 時自動標記 `paid`，並記錄收款者與收款時間。
- 前端訂單工作台與我的訂單歷史顯示 `未付款` / `已付款` badge。
- ready 訂單的櫃台操作文案改為「完成取餐並收款」。

### 不在本輪範圍

- 不做線上金流。
- 不新增付款方式欄位。
- 不做退款、付款取消、重開訂單。
- 不新增獨立 payment endpoint。

### 本機驗證

```bash
bun test
bun run build
bunx tsc --noEmit --skipLibCheck --moduleResolution bundler --module esnext --target esnext --jsx react-jsx --allowImportingTsExtensions backend.ts frontend/src/App.tsx tests/v10-menu-versioning.test.ts tests/v10-rbac.test.ts tests/v10-role-requests.test.ts tests/v10-admin-users.test.ts tests/v10-role-audit-logs.test.ts tests/v10-order-workbench.test.ts tests/v10-order-pickup-info.test.ts tests/v10-order-cancellation.test.ts tests/v10-order-payment.test.ts
git diff --check
```

### Render 驗證清單

- migration log 出現 `0006_v10_order_payment_status`。
- customer 送出訂單後，在歷史訂單看到 `未付款`。
- chef 推進到 `ready` 後仍是 `未付款`。
- staff 點「完成取餐並收款」後，訂單變成 `completed` 且顯示 `已付款`。
- customer 重新整理後，在歷史訂單看到 `已付款`。
- cancelled 訂單不會顯示收款操作。

## V10.4E 取餐叫號看板與顧客狀態提示

### 狀態更新

- `V10.4D 到店付款狀態` 已由使用者回報 Render 驗證成功。
- 本輪實作 `V10.4E 取餐叫號看板與顧客狀態提示`。
- 本輪不新增 migration，不導入 WebSocket。

### 本輪實作內容

- 新增公開 API：`GET /api/orders/pickup-board`。
- API 只回傳 `ready` 訂單，最多 20 筆，新到舊排序。
- API 只回傳公開展示欄位：`id`、`pickupCode`、`status`、`createdAt`、`createdAtTaipei`。
- 不回傳顧客識別、備註、餐點內容、金額或付款資訊。
- 前端新增「取餐叫號」區塊，未登入訪客也可查看。
- 顧客歷史訂單中，`ready` 訂單會顯示取餐提示。
- staff 完成取餐並收款後，訂單會從叫號看板消失。

### 不在本輪範圍

- 不做 WebSocket 即時推播。
- 不做叫號音效。
- 不做獨立後台頁或獨立看板路由。
- 不做退款、重開訂單。

### 本機驗證

```bash
bun test
bun run build
bunx tsc --noEmit --skipLibCheck --moduleResolution bundler --module esnext --target esnext --jsx react-jsx --allowImportingTsExtensions backend.ts frontend/src/App.tsx tests/v10-menu-versioning.test.ts tests/v10-rbac.test.ts tests/v10-role-requests.test.ts tests/v10-admin-users.test.ts tests/v10-role-audit-logs.test.ts tests/v10-order-workbench.test.ts tests/v10-order-pickup-info.test.ts tests/v10-order-cancellation.test.ts tests/v10-order-payment.test.ts tests/v10-pickup-board.test.ts
git diff --check
```

### Render 驗證清單

- 未登入訪客也能看到「取餐叫號」區塊。
- customer 送出訂單後，看板不顯示該訂單。
- chef 將訂單推進到 `ready` 後，看板顯示該訂單取餐編號。
- customer 在「我的訂單歷史」看到 ready 取餐提示。
- staff 點「完成取餐並收款」後，看板移除該訂單。
- `GET /api/orders/pickup-board` 不回傳個資、備註、餐點、金額或付款資訊。

## V10.4F 即時感自動刷新

### 狀態更新

- `V10.4E 取餐叫號看板與顧客狀態提示` 已由使用者回報 Render 驗證成功。
- 本輪實作 `V10.4F 即時感自動刷新`。
- 本輪採低風險 polling，不導入 WebSocket。

### 本輪實作內容

- 新增 `shared/auto-refresh.ts`，集中管理刷新間隔與刷新目標判斷。
- 取餐叫號看板每 10 秒自動刷新。
- 已登入使用者每 15 秒自動刷新目前購物車與我的訂單歷史。
- `staff/chef/owner/admin` 每 10 秒自動刷新訂單工作台。
- admin RBAC 管理資料不自動刷新，避免不必要 API 壓力。
- 使用者正在送出訂單、清空購物車、更新訂單狀態或取消訂單時，polling 會跳過該輪。
- 操作成功後仍立即重新載入相關資料，不等待下一輪 polling。

### 不在本輪範圍

- 不做 WebSocket。
- 不做背景頁暫停。
- 不做 exponential backoff。
- 不做 toast 通知。
- 不新增 API 或 migration。

### 本機驗證

```bash
bun test
bun run build
bunx tsc --noEmit --skipLibCheck --moduleResolution bundler --module esnext --target esnext --jsx react-jsx --allowImportingTsExtensions backend.ts frontend/src/App.tsx tests/v10-menu-versioning.test.ts tests/v10-rbac.test.ts tests/v10-role-requests.test.ts tests/v10-admin-users.test.ts tests/v10-role-audit-logs.test.ts tests/v10-order-workbench.test.ts tests/v10-order-pickup-info.test.ts tests/v10-order-cancellation.test.ts tests/v10-order-payment.test.ts tests/v10-pickup-board.test.ts tests/v10-auto-refresh.test.ts
git diff --check
```

### Render 驗證清單

- 未登入訪客停留在首頁，取餐叫號看板會自動刷新。
- customer 登入後，其他角色把訂單推進到 `ready`，顧客歷史訂單可在下一輪自動看到 ready 提示。
- chef/staff 工作台在其他人操作後會自動更新。
- staff 完成取餐並收款後，叫號看板在下一輪自動移除該訂單。
- 手動重新整理按鈕仍可正常使用。
## V10.4G 退款與取消訂單重開

狀態：本機實作中；`V10.4F 即時感自動刷新` 已由使用者回報 Render 驗證成功。

### 已規劃/實作項目

- 新增 migration：`drizzle-v10/0007_v10_refund_reopen_info.sql`
- 訂單新增退款欄位：
  - `refund_reason`
  - `refunded_by`
  - `refunded_at`
- `paymentStatus` 從 `unpaid | paid` 擴充為 `unpaid | paid | refunded`
- 新增退款 API：`PATCH /api/orders/:id/refund`
  - 需要 `staff/owner/admin`
  - 只允許 `completed + paid`
  - 成功後訂單仍為 `completed`，付款狀態改為 `refunded`
- 新增重開 API：`PATCH /api/orders/:id/reopen`
  - 需要 `staff/owner/admin`
  - 只允許 `cancelled -> submitted`
  - 重開後清空取消資訊，保留餐點、總金額、顧客備註與取餐編號
- 前端訂單工作台新增：
  - `退款`
  - `重開訂單`
  - 退款資訊顯示
- 顧客訂單歷史顯示 `已退款` badge 與退款資訊。

### 本機驗證清單

- `bun test`
- `bun run build`
- `bunx tsc --noEmit --skipLibCheck --moduleResolution bundler --module esnext --target esnext --jsx react-jsx --allowImportingTsExtensions backend.ts frontend/src/App.tsx tests/v10-menu-versioning.test.ts tests/v10-rbac.test.ts tests/v10-role-requests.test.ts tests/v10-admin-users.test.ts tests/v10-role-audit-logs.test.ts tests/v10-order-workbench.test.ts tests/v10-order-pickup-info.test.ts tests/v10-order-cancellation.test.ts tests/v10-order-payment.test.ts tests/v10-pickup-board.test.ts tests/v10-auto-refresh.test.ts tests/v10-refund-reopen.test.ts`
- `git diff --check`

### Render 驗證清單

- migration log 出現 `0007_v10_refund_reopen_info`
- staff 對 `completed + paid` 訂單退款後，顧客歷史訂單顯示 `已退款`
- refunded 訂單不能再次退款
- staff 對 cancelled 訂單按「重開訂單」後，訂單回到工作台 submitted 狀態
- chef 可繼續將重開訂單推進到 preparing/ready
- customer/chef 看不到退款或重開操作

## V10.4H 營運摘要看板

狀態：本機實作中；`V10.4G 退款與取消訂單重開` 已由使用者回報 Render 驗證成功。

### 實作目標

- 讓 staff/owner/admin 能快速看到營運狀態。
- 不新增 migration，直接從既有訂單資料推導摘要。
- chef 維持只處理訂單工作台，不查看營收摘要。
- customer 不可查看營運摘要。

### 已實作項目

- 新增 `OperationsSummary` contract。
- 新增 `GET /api/orders/operations-summary?range=today|all`。
- 摘要內容包含有效訂單、待處理、完成、取消、已付款、已退款、總收款、退款金額、淨營收、未收款金額與各訂單狀態固定計數。
- `today` 使用 Asia/Taipei 日期判斷。
- 前端新增「營運摘要」區塊，提供今日/全部切換、重新整理、摘要卡片與狀態計數。
- 新增測試：`tests/v10-operations-summary.test.ts`

### 本機驗證清單

- `bun test`
- `bun run build`
- `bunx tsc --noEmit --skipLibCheck --moduleResolution bundler --module esnext --target esnext --jsx react-jsx --allowImportingTsExtensions backend.ts frontend/src/App.tsx tests/v10-menu-versioning.test.ts tests/v10-rbac.test.ts tests/v10-role-requests.test.ts tests/v10-admin-users.test.ts tests/v10-role-audit-logs.test.ts tests/v10-order-workbench.test.ts tests/v10-order-pickup-info.test.ts tests/v10-order-cancellation.test.ts tests/v10-order-payment.test.ts tests/v10-pickup-board.test.ts tests/v10-auto-refresh.test.ts tests/v10-refund-reopen.test.ts tests/v10-operations-summary.test.ts`
- `git diff --check`

### Render 驗證清單

- staff/owner/admin 登入後看到「營運摘要」
- customer/chef 看不到「營運摘要」
- customer 送出訂單後，staff 的今日摘要有效訂單與未收款金額增加
- staff 完成取餐並收款後，淨營收增加
- staff 退款後，退款金額增加且淨營收下降
- 今日/全部切換可正常更新數字

## V10.5A 菜單顯示排序

狀態：本機實作中；`V10.4H 營運摘要看板` 已由使用者回報 Render 驗證成功。

### 實作目標

- owner/admin 可調整公開菜單顯示順序。
- customer 只看到排序後的菜單，不看到排序控制。
- 編輯菜單建立新版後，同一品項保留原排序位置。
- 新增 `接下來可能的任務清單.md`，只作為人類閱讀與下一個 session 交接。

### 已實作項目

- 新增 migration：`drizzle-v10/0008_v10_menu_display_order.sql`
- `menu_items` 新增 `display_order integer not null default 0`
- `MenuItem` contract 新增 `displayOrder`
- `GET /api/menu` 改為依 `displayOrder asc, id asc` 排序
- `POST /api/menu` 可選 `display_order`
- `PATCH /api/menu/:id` 建立新版時保留原 `displayOrder`
- 新增 `PATCH /api/menu/reorder`
- PostgreSQL store 與 JSON fallback store 都支援排序
- 前端菜單管理區新增排序值與「上移 / 下移」操作
- 新增測試：`tests/v10-menu-display-order.test.ts`

### Render 驗證清單

- migration log 出現 `0008_v10_menu_display_order`
- owner/admin 登入後可在菜單管理區看到上移/下移
- 調整排序後，公開菜單順序同步改變
- 編輯某品項產生新版後，該品項排序位置不變
- customer 看不到排序控制，但看到排序後菜單
- 根目錄存在 `接下來可能的任務清單.md`

## V10.5B 菜單分類與售完/隱藏狀態

狀態：本機實作中；`V10.5A 菜單顯示排序` 已由使用者回報 Render 驗證成功。

### 實作目標

- owner/admin 可透過菜單管理切換品項售完與隱藏狀態。
- 售完品項仍出現在公開菜單，但不可加入購物車。
- 隱藏品項不出現在公開菜單，但仍在管理清單可見。
- 編輯建立新版時保留 display order，並更新售完/隱藏狀態。

### 已實作項目

- 新增 migration：`drizzle-v10/0009_v10_menu_availability.sql`
- `menu_items` 新增 `is_sold_out`、`is_hidden`
- `MenuItem` contract 新增 `isSoldOut`、`isHidden`
- 新增 `GET /api/menu/admin`
- `GET /api/menu` 過濾 hidden 品項
- 購物車加入與送出時拒絕售完/隱藏品項
- 前端菜單管理表單新增「售完」「隱藏」checkbox
- 公開菜單售完品項顯示售完 badge 並禁用加入購物車
- 新增測試：`tests/v10-menu-availability.test.ts`

### Render 驗證清單

- migration log 出現 `0009_v10_menu_availability`
- owner/admin 可在菜單管理區切換售完與隱藏
- 售完品項仍顯示在公開菜單，但不能加入購物車
- 隱藏品項不顯示在公開菜單
- 取消隱藏後，品項回到原本排序位置
- customer 無法透過舊畫面或舊購物車送出已售完/隱藏品項
## 最新進度：V10.5C + V10.6A 合併實作

使用者已回報 `V10.5B 菜單分類與售完/隱藏狀態` Render 驗證成功。本輪開始採一次實作兩個相鄰階段，提高部署與線上驗證效率。

### V10.5C 單品特價與折扣價基礎

- 新增 migration：`drizzle-v10/0010_v10_menu_sale_price.sql`
- `menu_items` 新增 `sale_price` 與 `promotion_label`
- `MenuItem` 新增 `salePrice` 與 `promotionLabel`
- owner/admin 可在菜單管理表單設定特價與促銷標籤
- 特價必須大於 0 且低於原價；空白或 `null` 代表取消特價
- 購物車、送出訂單、訂單歷史、工作台明細使用有效價格：`salePrice ?? price`
- 特價跟著菜單版本保存，舊訂單可保留當時價格

### V10.6A 營運報表 CSV 匯出

- 新增 `GET /api/reports/orders.csv?range=today|all`
- 權限限定 `staff/owner/admin`
- `today` 使用 Asia/Taipei 日期切分，和營運摘要一致
- CSV 欄位包含訂單 ID、取餐編號、狀態、付款狀態、總額、品項摘要與主要時間欄位
- CSV 不輸出使用者 email、roles、OAuth 資料
- 前端營運摘要區新增「匯出 CSV」按鈕

### 本輪驗證清單

- `bun test`
- `bun run build`
- 入口檔 TypeScript check 加入：
  - `tests/v10-menu-promotions.test.ts`
  - `tests/v10-orders-csv-export.test.ts`
- Render 驗證：
  - migration log 出現 `0010_v10_menu_sale_price`
  - owner/admin 可設定與取消特價
  - customer 加入特價品項後 cart/order total 使用特價
  - staff/owner/admin 可下載 CSV
  - customer/chef 無法使用 CSV 匯出
# 專案 V10 最新進度：V10.3 菜單版本化進階功能

## 背景

使用者已回報 `V10.5C 單品特價與折扣價基礎` 與 `V10.6A 營運報表 CSV 匯出` 已完成 Render 驗證。本輪回到老師講義的 V10.3 進階功能，挑選三個能在目前專案中穩定完成、且不破壞既有下單流程的子任務。

## 本輪已實作

- 分級版本管理：
  - 新增 `majorVersion`、`minorVersion`、`versionNote`
  - `PATCH /api/menu/:id` 支援 `version_level: "minor" | "major"`
  - minor 更新會讓 minor + 1；major 更新會讓 major + 1 並重設 minor
  - 保留既有版本 ID 格式，例如 `001-02`
- 價格敏感度分析：
  - 新增 `GET /api/menu/:id/price-analysis`
  - owner/admin 可查看同 logical item 各版本價格、特價、訂單數、銷售份數、營收、平均單價
  - 前端菜單管理加入 `Price analysis` 按鈕與摘要表格
- A/B 測試最小切片：
  - 新增 `experimentKey`、`experimentVariant`
  - owner/admin 可在菜單表單填入 A/B metadata
  - 新增 `GET /api/menu/experiments`
  - 前端顯示 active experiments 的 variant、品項數、訂單數、銷售份數與營收
  - 本輪不做顧客隨機分流，因此 customer 公開菜單與下單流程維持穩定

## 新增 migration

- `drizzle-v10/0011_v10_menu_semantic_versions.sql`
- `drizzle-v10/0012_v10_menu_ab_test_metadata.sql`

## 新增測試

- `tests/v10-menu-semantic-version.test.ts`
- `tests/v10-price-analysis.test.ts`
- `tests/v10-menu-ab-test.test.ts`

## 本機驗證

- `bun test`：通過，79 pass
- `bun run build`：通過
- 入口檔 TypeScript check：通過

## Render 驗證建議

- migration log 出現 `0011_v10_menu_semantic_versions`
- migration log 出現 `0012_v10_menu_ab_test_metadata`
- owner/admin 編輯品項時可選 major/minor 更新
- 價格分析能看到同一品項不同版本的銷售統計
- owner/admin 可設定 experiment key/variant，並在 A/B experiments 摘要看到彙整
- customer 下單流程不受 A/B metadata 影響

---
