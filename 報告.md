# 早餐店訂餐系統 V10 實作報告

## 摘要

本專案已完成 V10.1 到 V10.4B，且使用者已回報 Render 線上驗證成功。
本輪新增 `V10.4C 訂單取消流程`，讓訂單生命週期從送出、製作、完成，再補上保守且可展示的取消規則。

## 已完成且已 Render 驗證

### V10.1 基礎版本化

- 菜單品項改為版本化 ID，例如 `001-01`、`001-02`。
- 訂單引用特定菜單版本。
- 舊版菜單不可加入購物車。
- 舊購物車送出時會回 `409`。

### V10.2 菜單管理與版本展示

- 登入後可新增、編輯、下架菜單。
- 新增 `GET /api/menu/:id/history`。
- 菜單卡片顯示 logical id、版本 badge、近期更新提示。
- 圖片 URL 預覽與錯誤提示已修正。

### V10.3A RBAC 基礎

- `user.roles` 欄位已建立。
- `RBAC_ADMIN_EMAILS` 可自動授予初始 admin。
- 菜單管理需要 `owner/admin`。
- 查看所有訂單需要 `staff/chef/owner/admin`。

### V10.3B 角色申請與審核

- 使用者可申請 `staff/chef`。
- admin 可核准或拒絕申請。
- 核准後角色會合併進使用者 roles。

### V10.3C Admin 使用者角色管理

- admin 可查看使用者列表。
- admin 可直接調整使用者 roles。
- `customer` 角色永遠保留。
- admin 不可移除自己的 `admin` role。

### V10.3D RBAC 審計紀錄

- 新增 `role_audit_logs`。
- admin 核准/拒絕角色申請會留下紀錄。
- admin 直接更新 roles 會留下紀錄。
- 前端可查看角色異動紀錄。

### V10.4A 店員/廚房訂單工作台

- 訂單狀態擴充為 `pending | submitted | preparing | ready | completed`。
- 新增 `GET /api/orders/workbench`。
- 新增 `PATCH /api/orders/:id/status`。
- `chef/owner/admin` 可將訂單推進到製作中與等待取餐。
- `staff/owner/admin` 可將 ready 訂單完成取餐。
- 顧客歷史訂單會顯示真實狀態。

### V10.4B 取餐資訊與顧客備註

- 新增 `orders.customer_note`。
- `OrderResponse` 新增 `pickupCode`，格式為 `A-0007`。
- 顧客送出訂單時可填寫 120 字內備註。
- 工作台與我的訂單歷史都顯示取餐編號與備註。

## V10.4C 訂單取消流程

### 本輪新增

- 新增 migration：`drizzle-v10/0005_v10_order_cancellation_info.sql`。
- `Order.status` 新增 `cancelled`。
- `orders` 新增：
  - `cancel_reason text not null default ''`
  - `cancelled_by text null references user(id)`
  - `cancelled_at timestamp with time zone null`
- `Order` / `OrderResponse` 新增 `cancelReason`、`cancelledBy`、`cancelledAt`。
- 新增 `PATCH /api/orders/:id/cancel`。
- 新增 `cancelOrderBodySchema`：`{ reason?: string }`，trim 後最多 120 字。
- PostgreSQL store 與 JSON store 都支援取消資訊。
- 前端歷史訂單與工作台都支援取消操作與取消資訊顯示。

### 取消規則

- customer 只能取消自己的 `submitted` 訂單。
- `staff/owner/admin` 可取消 `submitted/preparing/ready` 訂單。
- `pending/completed/cancelled` 不可取消。
- chef 不負責取消訂單。
- `cancelled` 是終態，不能再推進狀態或再次取消。

### 不做項目

- 不做退款。
- 不做付款狀態。
- 不做重開訂單。
- 不做取消原因 modal。
- 不做 WebSocket 即時推播。
- 不新增獨立取消 audit table。

## API 摘要

### `PATCH /api/orders/:id/cancel`

- 需要登入。
- body 可省略。
- body 可選 `reason`，最多 120 字。
- 找不到訂單回 `404`。
- 權限不足回 `403`。
- 狀態不可取消回 `409`。
- 成功後訂單狀態變為 `cancelled`，並保存取消者、取消時間與取消原因。

### 所有 Order response

- 回傳 `customerNote`。
- 回傳 `pickupCode`。
- 回傳 `cancelReason`。
- 回傳 `cancelledBy`。
- 回傳 `cancelledAt`。

## 本機驗證命令

```bash
bun test
bun run build
bunx tsc --noEmit --skipLibCheck --moduleResolution bundler --module esnext --target esnext --jsx react-jsx --allowImportingTsExtensions backend.ts frontend/src/App.tsx tests/v10-menu-versioning.test.ts tests/v10-rbac.test.ts tests/v10-role-requests.test.ts tests/v10-admin-users.test.ts tests/v10-role-audit-logs.test.ts tests/v10-order-workbench.test.ts tests/v10-order-pickup-info.test.ts tests/v10-order-cancellation.test.ts
git diff --check
```

## Render 驗證清單

V10.4C 部署後請確認：

- migration log 出現 `0005_v10_order_cancellation_info`。
- customer 送出訂單後，可在「我的訂單歷史」取消 `submitted` 訂單。
- customer 無法取消已進入 `preparing/ready/completed` 的訂單。
- staff 可在工作台取消 `submitted/preparing/ready` 訂單。
- chef 看不到取消操作，或呼叫取消 API 回 `403`。
- 取消後訂單顯示「已取消」，且不再出現下一步製作/完成按鈕。

## 後續可做

### 營運流程

- 訂單付款。
- 訂單退款。
- 重開訂單。
- 叫號或取餐通知。
- WebSocket 即時推播。

### 後台管理

- audit log 搜尋與匯出。
- admin 使用者搜尋/分頁。
- 使用者角色變更備註強化。

### 菜單與行銷

- display order。
- major/minor version。
- 版本比較 UI。
- 促銷系統。
- A/B testing。
