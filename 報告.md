# 早餐店訂餐系統 V10 實作報告

## 摘要

本專案已從 V9 延伸到 V10，主軸是「菜單版本化」與「RBAC 角色權限」。目前 V10.1、V10.2、V10.3A、V10.3B 都已由使用者回報完成 Render 線上驗證。本輪實作進入 `V10.3C Admin 使用者角色管理`，補上 admin 直接查看使用者列表並調整 roles 的能力。

目前 V10 已實作：

- 菜單品項版本化
- 訂單引用特定菜單版本
- 菜單管理 UI
- 菜單版本歷史展示
- 圖片 URL 預覽與載入失敗提示
- RBAC roles 基礎
- 初始 admin bootstrap
- staff/chef 角色申請
- admin 角色申請審核
- admin 直接使用者角色管理

## V10.1 基礎版本化

### 已完成

- PostgreSQL schema 改用 `bf_v10`
- V10 migration 改放在 `drizzle-v10/`
- `menu_items` 改為版本化模型：
  - `id` 是版本 ID，例如 `001-01`、`001-02`
  - `logical_id` 表示同一個菜單品項
  - `version` 表示版本號
  - `is_current_version` 標記目前可販售版本
  - `supersedes` 與 `change_reason` 記錄版本來源與修改原因
- `order_items.menu_item_id` 指向特定 `menu_items.id`
- 更新菜單時建立新版，不覆蓋舊版
- 下架菜單時只讓 current version 失效，不實體刪除
- 加入購物車與送出訂單時檢查品項是否仍為 current version
- 舊購物車送出時回 `409`

### 已驗證

- Render build/runtime 成功
- Neon migration 成功建立 `bf_v10`
- 線上可讀菜單、改價、下單
- 歷史訂單保留當時版本的價格與名稱

## V10.2 菜單管理與版本展示

### 已完成

- 前端新增登入後可見的菜單管理 UI
- 可新增、編輯、下架品項
- 每個品項顯示 logical id、版本 ID、版本 badge
- 修改品項會呼叫 `PATCH /api/menu/:id`
- 下架品項會呼叫 `DELETE /api/menu/:id`
- 新增 `GET /api/menu/:id/history`
- 歷史版本由新到舊顯示
- 編輯時可填寫 `change_reason`
- 菜單卡片若 `version > 1` 會顯示近期更新提示

### 圖片 UX 修正

- 表單新增圖片 URL 即時預覽
- 圖片載入失敗時明確顯示「圖片載入失敗」
- 顯示原始圖片網址的「開啟原圖」連結
- fallback 圖仍保留，但不再靜默取代使用者輸入的 URL
- 圖片 URL 驗證維持寬鬆，允許公開 https URL 與相對路徑

### 已驗證

- Render 線上可新增、改價、下架菜單
- `/api/menu` 會保留使用者輸入的 `image_url`
- 有效圖片 URL 可顯示
- 無效圖片 URL 會顯示錯誤提示，不會誤導為沒有儲存

## V10.3A RBAC 基礎

### 已完成

- `bf_v10.user` 新增 `roles text[] not null default ARRAY['customer']`
- 新增 migration：`drizzle-v10/0001_v10_rbac_roles.sql`
- 固定角色：
  - `customer`
  - `staff`
  - `chef`
  - `owner`
  - `admin`
- `SessionUser` 新增 `roles`
- 新增 `GET /api/users/me`
- `RBAC_ADMIN_EMAILS` 會在登入後自動授予 `admin`
- 新增 RBAC guards：
  - `hasRole`
  - `hasAnyRole`
  - `hasAllRoles`
  - `requireAnyRole`
- 菜單 mutation 需要 `owner/admin`
- `GET /api/orders` 需要 `staff/chef/owner/admin`
- 一般 `customer` 仍可瀏覽菜單、使用購物車、送出訂單、查看自己的訂單
- 前端依 roles 顯示或隱藏菜單管理 UI

### 已驗證

- Render env 設定 `RBAC_ADMIN_EMAILS`
- admin 登入後 navbar 顯示 admin role
- admin 可管理菜單
- 非 admin 使用者看不到菜單管理區
- `/api/users/me` 線上回傳 roles

## V10.3B 角色申請與 Admin 審核

### 已完成

- 新增 `role_requests` table
- 新增 migration：`drizzle-v10/0002_v10_role_requests.sql`
- 新增 role request contracts 與 route schemas
- 一般使用者可申請：
  - `staff`
  - `chef`
- 同一使用者同時間只能有一筆 pending 申請
- 已擁有某角色時不可再申請同角色
- 新增 API：
  - `POST /api/users/me/role-request`
  - `GET /api/users/me/role-requests`
  - `GET /api/admin/role-requests`
  - `PATCH /api/admin/role-requests/:id`
- admin 可核准或拒絕 pending 申請
- 核准後會把 requested role 合併到使用者 roles
- roles 合併時會保留 `customer` 並去重
- 前端新增「角色申請」與「角色申請審核」卡片

### 已驗證

使用者已回報下列項目皆已透過 Render 驗證完成：

- admin 驗證成功
- staff/chef 角色申請成功
- admin 核准機制成功
- admin 拒絕機制成功
- 一般使用者重新整理或重新登入後 roles 正確更新

## V10.3C Admin 使用者角色管理

### 本輪新增

- 新增 admin user contract：
  - `adminUserSchema`
  - `AdminUser`
- 新增 admin user route schemas：
  - `adminUserResponseSchema`
  - `adminUserListResponseSchema`
  - `updateAdminUserRolesParamsSchema`
  - `updateAdminUserRolesBodySchema`
- 新增共用 roles 正規化：
  - `normalizeRoles(value)`
  - 永遠保留 `customer`
  - 移除重複 roles
  - 過濾非法 roles
- 新增 API：
  - `GET /api/admin/users`
  - `PATCH /api/admin/users/:userId/roles`
- `GET /api/admin/users`
  - 需要 `admin`
  - 回傳 `id`、`name`、`email`、`roles`、`createdAt`、`updatedAt`
  - 依 email 穩定排序
- `PATCH /api/admin/users/:userId/roles`
  - 需要 `admin`
  - body：`{ roles: Role[] }`
  - roles 只允許 `customer | staff | chef | owner | admin`
  - 後端自動保留 `customer` 並去重
  - 找不到使用者回 `404`
  - admin 嘗試移除自己的 `admin` role 回 `409`
- 前端新增 admin-only「使用者角色管理」卡片：
  - 顯示所有使用者姓名、email、roles
  - 使用 checkbox 編輯 roles
  - `customer` checkbox 固定不可取消
  - 自己帳號的 `admin` checkbox 固定不可取消
  - 儲存成功後重新載入使用者列表
  - 若更新的是目前登入者，會重新呼叫 `/api/users/me`

### 本輪不做

- 不新增 migration
- 不新增審計紀錄表
- 不做使用者搜尋或分頁
- 不做 owner/admin 自助申請
- 不做獨立後台 router

### 本機驗證

建議執行：

```bash
bun test
bun run build
bunx tsc --noEmit --skipLibCheck --moduleResolution bundler --module esnext --target esnext --jsx react-jsx --allowImportingTsExtensions backend.ts frontend/src/App.tsx tests/v10-menu-versioning.test.ts tests/v10-rbac.test.ts tests/v10-role-requests.test.ts tests/v10-admin-users.test.ts
git diff --check
```

### Render 驗證清單

部署後請在線上確認：

- admin 登入後看到「使用者角色管理」區塊
- admin 可讀取使用者列表
- 非 admin 不能呼叫 admin users API
- admin 可把一般使用者加上 `owner`
- 該使用者重新整理或重新登入後可看到菜單管理區
- admin 可移除一般使用者的 `owner`
- 該使用者重新整理後看不到菜單管理區
- admin 無法移除自己的 `admin` role

## 目前 API 摘要

### 菜單

- `GET /api/menu`：公開，取得 current menu items
- `POST /api/menu`：需要 `owner/admin`
- `PATCH /api/menu/:id`：需要 `owner/admin`
- `DELETE /api/menu/:id`：需要 `owner/admin`
- `GET /api/menu/:id/history`：公開，取得版本歷史

### 使用者與 RBAC

- `GET /api/users/me`：需要登入，取得目前使用者與 roles
- `POST /api/users/me/role-request`：需要登入，申請 `staff/chef`
- `GET /api/users/me/role-requests`：需要登入，取得自己的申請紀錄
- `GET /api/admin/role-requests`：需要 `admin`
- `PATCH /api/admin/role-requests/:id`：需要 `admin`
- `GET /api/admin/users`：需要 `admin`
- `PATCH /api/admin/users/:userId/roles`：需要 `admin`

### 訂單

- 顧客訂單與購物車 API 需要登入
- `GET /api/orders` 需要 `staff/chef/owner/admin`
- 加入購物車只接受 current menu version
- 送出訂單時若品項過期會回 `409`

## 後續候選任務

### RBAC 後續

- 角色變更審計紀錄表
- 使用者列表搜尋與分頁
- admin 操作紀錄
- owner/admin 申請或交接流程

### 菜單後續

- display order
- major/minor version
- 版本比較 UI
- 促銷系統
- A/B testing

### 作業流程後續

- 店員/廚房專用訂單工作台
- 訂單狀態流程細分
- 出餐狀態與通知

### 測試後續

- 加強 API integration smoke tests
- 加強登入後權限切換測試
- 加強 stale cart 與歷史訂單回歸測試
