# 早餐店訂餐系統 V10 實作報告

## 摘要

本專案已完成 V10.1 到 V10.4A，且使用者已回報 Render 線上驗證成功。
本輪新增 `V10.4B 取餐資訊與顧客備註`，讓訂單從「狀態可流轉」再往真實店面流程前進：顧客送出訂單時可留下備註，店員/廚房可用取餐編號辨識訂單。

## 已完成且已 Render 驗證

### V10.1 基礎版本化

- 菜單品項改為版本化 ID，例如 `001-01`、`001-02`。
- 訂單引用特定菜單版本。
- 舊版菜單不可加入購物車。
- 舊購物車送出時會回 `409`。

### V10.2 菜單管理與版本展示

- 登入後可新增、編輯、下架菜單。
- 新增 `GET /api/menu/:id/history`。
- 菜單卡片顯示 logical id、版本 badge、近期更新提示。
- 圖片 URL 預覽與錯誤提示已修正。

### V10.3A RBAC 基礎

- `user.roles` 欄位已建立。
- `RBAC_ADMIN_EMAILS` 可自動授予初始 admin。
- 菜單管理需要 `owner/admin`。
- 查看所有訂單需要 `staff/chef/owner/admin`。

### V10.3B 角色申請與審核

- 使用者可申請 `staff/chef`。
- admin 可核准或拒絕申請。
- 核准後角色會合併進使用者 roles。

### V10.3C Admin 使用者角色管理

- admin 可查看使用者列表。
- admin 可直接調整使用者 roles。
- `customer` 角色永遠保留。
- admin 不可移除自己的 `admin` role。

### V10.3D RBAC 審計紀錄

- 新增 `role_audit_logs`。
- admin 核准/拒絕角色申請會留下紀錄。
- admin 直接更新 roles 會留下紀錄。
- 前端可查看角色異動紀錄。

### V10.4A 店員/廚房訂單工作台

- 訂單狀態擴充為 `pending | submitted | preparing | ready | completed`。
- 新增 `GET /api/orders/workbench`。
- 新增 `PATCH /api/orders/:id/status`。
- `chef/owner/admin` 可將訂單推進到製作中與等待取餐。
- `staff/owner/admin` 可將 ready 訂單完成取餐。
- 顧客歷史訂單會顯示真實狀態。

## V10.4B 取餐資訊與顧客備註

### 本輪新增

- 新增 migration：`drizzle-v10/0004_v10_order_pickup_info.sql`。
- `orders` 新增 `customer_note text not null default ''`。
- `Order` 新增 `customerNote`。
- `OrderResponse` 新增 `pickupCode`，格式為 `A-0007`。
- `pickupCode` 不存 DB，由 `order.id` 穩定推導。
- `POST /api/orders/:id/submit` 可接受：

```json
{ "customerNote": "不要辣，餐點分袋" }
```

- 備註最多 120 字，空白會 trim，未填則存成空字串。
- PostgreSQL store 與 JSON store 都支援 `customerNote`。
- 工作台顯示取餐編號與備註。
- 我的訂單歷史顯示取餐編號與備註。

### 不做項目

- 不做付款狀態。
- 不做桌號。
- 不做叫號通知。
- 不做訂單取消、退款、重開訂單。
- 不做 WebSocket 即時推播。

## API 摘要

### `POST /api/orders/:id/submit`

- 需要登入。
- 只允許送出自己的 `pending` 訂單。
- body 可省略。
- body 可選 `customerNote`，最多 120 字。
- 成功後訂單狀態變為 `submitted`，並保存備註。

### 所有 Order response

- 回傳 `customerNote`。
- 回傳 `pickupCode`。
- 既有 `createdAtTaipei` 保留。

## 本機驗證命令

```bash
bun test
bun run build
bunx tsc --noEmit --skipLibCheck --moduleResolution bundler --module esnext --target esnext --jsx react-jsx --allowImportingTsExtensions backend.ts frontend/src/App.tsx tests/v10-menu-versioning.test.ts tests/v10-rbac.test.ts tests/v10-role-requests.test.ts tests/v10-admin-users.test.ts tests/v10-role-audit-logs.test.ts tests/v10-order-workbench.test.ts tests/v10-order-pickup-info.test.ts
git diff --check
```

## Render 驗證清單

V10.4B 部署後請確認：

- Render migration log 出現 `0004_v10_order_pickup_info`。
- customer 填寫備註並送出訂單。
- customer 在「我的訂單歷史」看到取餐編號與備註。
- chef/staff 在「訂單工作台」看到同一筆訂單的取餐編號與備註。
- chef/staff 完成 V10.4A 狀態流轉後，備註與取餐編號仍保留。
- 未填備註時，工作台與歷史訂單顯示無備註狀態。

## 後續可做

### 營運流程

- 訂單付款。
- 訂單取消、退款、重開訂單。
- 叫號或取餐通知。
- WebSocket 即時推播。

### 後台管理

- audit log 搜尋與匯出。
- admin 使用者搜尋/分頁。
- 使用者角色變更備註強化。

### 菜單與行銷

- display order。
- major/minor version。
- 版本比較 UI。
- 促銷系統。
- A/B testing。
