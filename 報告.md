# 早餐店訂餐系統 V10 實作報告

## 摘要

本專案已完成 V10.1 到 V10.3D，且使用者已回報 Render 線上驗證成功。
本輪新增 `V10.4A 店員/廚房訂單工作台`，讓 RBAC 不只停留在權限管理，而能實際進入營運流程：顧客送出訂單後，廚房製作、餐點完成、店員完成取餐。

## 已完成並通過 Render 驗證

### V10.1 基礎版本化

- 菜單品項版本化。
- 訂單引用特定菜單版本。
- 舊版菜單不可加入購物車。
- 舊購物車送出時回 `409`。

### V10.2 菜單管理與版本展示

- 登入後可新增、編輯、下架菜單。
- 新增 `GET /api/menu/:id/history`。
- 顯示 logical id、版本 badge、版本歷史。
- 圖片 URL 預覽與載入失敗提示已修正。

### V10.3A RBAC 基礎

- `user.roles` 欄位完成。
- `RBAC_ADMIN_EMAILS` 可自動授予初始 admin。
- 菜單管理需要 `owner/admin`。
- 查看所有訂單需要 `staff/chef/owner/admin`。

### V10.3B 角色申請與審核

- 使用者可申請 `staff/chef`。
- admin 可核准或拒絕申請。
- 核准後角色會合併進使用者 roles。

### V10.3C Admin 使用者角色管理

- admin 可查看使用者列表。
- admin 可直接調整使用者 roles。
- `customer` 永遠保留。
- admin 不能移除自己的 `admin` role。

### V10.3D RBAC 審計紀錄

- 新增 `role_audit_logs`。
- admin 核准/拒絕角色申請會留下紀錄。
- admin 直接更新 roles 會留下紀錄。
- 前端可查看角色異動紀錄。

## V10.4A 店員/廚房訂單工作台

### 本輪新增

- 訂單狀態從 `pending | submitted` 擴充為：
  - `pending`：顧客購物車
  - `submitted`：顧客已送出，等待處理
  - `preparing`：廚房製作中
  - `ready`：餐點完成，等待取餐
  - `completed`：已取餐/結案
- 新增 `OrderStatus` 與 `orderStatusSchema`。
- 新增 `GET /api/orders/workbench`。
- 新增 `PATCH /api/orders/:id/status`。
- Store 介面新增：
  - `getWorkbenchOrders()`
  - `updateOrderStatus(orderId, nextStatus)`
- PostgreSQL store 與 JSON store 都支援新狀態。
- PgStore reload 時會保留合法工作流狀態，不再把非 `submitted` 狀態壓回 `pending`。
- 前端新增「訂單工作台」區塊。
- 顧客歷史訂單會顯示真實狀態。

### 狀態與角色規則

- `chef/owner/admin` 可處理：
  - `submitted -> preparing`
  - `preparing -> ready`
- `staff/owner/admin` 可處理：
  - `ready -> completed`
- 不允許跳階。
- 不允許回退。
- 不允許修改 `pending` 購物車。
- 不允許修改 `completed` 訂單。

### 本輪不做

- 不新增 migration，因為 `orders.status` 已是 text。
- 不做付款、桌號、叫號、取餐通知。
- 不做訂單取消、退款、重開訂單。
- 不做 WebSocket 即時推播。
- 不新增獨立 router 或後台頁。

## API 摘要

### `GET /api/orders/workbench`

- 需要 `staff/chef/owner/admin`。
- 回傳非 `pending` 訂單。
- 依建立時間新到舊排序。

### `PATCH /api/orders/:id/status`

- 需要登入與符合角色權限。
- body：
  ```json
  { "status": "preparing" }
  ```
- `status` 只接受：
  - `preparing`
  - `ready`
  - `completed`
- 違反狀態流程回 `409`。
- 角色不足回 `403`。

## 本機驗證命令

```bash
bun test
bun run build
bunx tsc --noEmit --skipLibCheck --moduleResolution bundler --module esnext --target esnext --jsx react-jsx --allowImportingTsExtensions backend.ts frontend/src/App.tsx tests/v10-menu-versioning.test.ts tests/v10-rbac.test.ts tests/v10-role-requests.test.ts tests/v10-admin-users.test.ts tests/v10-role-audit-logs.test.ts tests/v10-order-workbench.test.ts
git diff --check
```

## Render 驗證清單

V10.4A 部署後請確認：

- 顧客送出訂單後，staff/chef/owner/admin 可在「訂單工作台」看到該訂單。
- chef 可將訂單改為「製作中」。
- chef 可將訂單改為「等待取餐」。
- staff 可將訂單改為「已完成」。
- 顧客重新整理後，在「我的訂單歷史」看到狀態變化。
- customer 看不到「訂單工作台」。
- staff 不可將 `submitted` 改為 `preparing`。
- chef 不可將 `ready` 改為 `completed`。

## 後續候選任務

### 營運流程

- 訂單付款。
- 桌號或取餐號。
- 訂單取消、退款、重開訂單。
- WebSocket 即時推播。

### 後台管理

- audit log 分頁與匯出。
- admin 使用者搜尋與分頁。
- 使用者角色變更備註。

### 菜單功能

- display order。
- major/minor version。
- 版本比較 UI。
- 促銷系統。
- A/B testing。
