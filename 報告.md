# 早餐店訂餐系統 V10 實作報告

## 摘要

本專案目前已完成第十週 V10 主線中的 `V10.1 基礎版本化` 與 `V10.2 菜單管理與版本展示`，並已由使用者在 Render 線上環境完成驗證。

`V10.3A RBAC 基礎切片` 已完成並由使用者在 Render 線上環境完全驗證成功。

本輪新增實作 `V10.3B 角色申請與 Admin 審核`，讓一般使用者可申請 `staff/chef`，並由 `admin` 核准或拒絕。此切片目前已在本機實作，待使用者推上 Render 做線上驗證。

目前系統已能支援：

- 菜單品項版本化。
- 訂單引用特定菜單版本。
- 過期菜單版本不可再加入購物車或結帳。
- 登入後從網站直接新增、編輯、下架菜單。
- 查看菜單版本歷史。
- 顯示版本提示、改價原因與圖片 URL 載入狀態。
- 使用者角色 roles。
- admin/owner 才能管理菜單。
- customer 可正常瀏覽、下單與查看自己的訂單。

下一階段若繼續實作，建議進入 `V10.3C`，重點會是 admin 直接角色管理、使用者角色管理後台，或再往後做排序、促銷等進階功能。

## 專案架構

- 前端：React + Vite + Tailwind/daisyUI。
- 後端：Bun + TypeScript + Elysia。
- 資料庫：Neon PostgreSQL。
- ORM：Drizzle ORM。
- 登入系統：Better Auth + Google OAuth。
- 部署平台：Render。
- V10 主要 schema：`bf_v10`。

## V10.1 基礎版本化

### 目標

V10.1 的核心目標是讓菜單品項不再只是單一資料，而是可以保留歷史版本。當菜單價格、名稱、描述或圖片改變時，系統會建立新版，而不是覆蓋舊資料。

這樣可以解決一個重要問題：歷史訂單應該顯示當時下單時的品項名稱與價格，而不是被後來的菜單修改影響。

### 已完成任務

- 建立 V10 專用 schema `bf_v10`。
- 建立 V10 專用 migration 目錄 `drizzle-v10/`。
- 建立乾淨的 V10 初始 migration。
- 將 `menu_items` 改成版本化資料模型。
- 將菜單版本 ID 設計為字串，例如 `001-01`、`001-02`。
- 新增 `logical_id`，用來表示同一個菜單品項。
- 新增 `version`，用來表示該品項的版本序號。
- 新增 `is_current_version`，用來判斷目前可售版本。
- 新增 `supersedes`，用來連結上一個版本。
- 新增 `change_reason`，用來記錄修改原因。
- 將 `order_items.menu_item_id` 改為指向特定版本的 `menu_items.id`。
- 建立 `menuRepository`，集中處理菜單版本邏輯。
- PostgreSQL store 已支援版本化。
- JSON file store 也已支援基本版本化，避免非 Postgres 模式下型別或 build 出錯。
- 新增菜單時會建立初版。
- 編輯菜單時會建立新版，不覆蓋舊版。
- 下架菜單時會讓 current version 失效，不實體刪除歷史資料。
- 加入購物車時只允許 current version。
- 送出訂單時再次檢查購物車內版本是否仍為 current。
- 若購物車內有舊版品項，送出訂單會回 `409`。
- 歷史訂單會透過版本 ID 找回當時購買的菜單版本。
- 前端購物車已支援字串版本 ID。
- 前端已能處理菜單版本過期的錯誤提示。

### 已驗證結果

- Render build 成功。
- Render runtime 成功啟動。
- Neon migration 成功建立或沿用 `bf_v10`。
- `GET /api/menu` 可取得 current menu items。
- 編輯品項後會產生新版。
- 舊版不再是 current version。
- 舊版不可再加入購物車或結帳。
- 歷史訂單仍能顯示當時版本資料。

## V10.2 菜單管理與版本展示

### 目標

V10.2 的核心目標是讓 V10.1 的版本化功能能被展示與操作。也就是說，使用者不只是在 API 層看見版本化，而是可以在 Render 網站上直接管理菜單，並看出每次修改都產生了版本歷史。

### 已完成任務

- 前端新增登入後可見的菜單管理區塊。
- 菜單管理區可列出目前 current menu items。
- 每個品項可顯示 logical id。
- 每個品項可顯示版本 ID，例如 `001-02`。
- 每個品項可顯示版本 badge，例如 `v2`。
- 當版本大於 1 時，前端會顯示近期調整提示。
- 新增品項表單已完成。
- 編輯品項表單已完成。
- 下架品項操作已完成。
- 編輯品項時會送出 `PATCH /api/menu/:id`。
- 下架品項時會送出 `DELETE /api/menu/:id`。
- 操作成功後會重新載入 `/api/menu`。
- 菜單 mutation endpoint 在 V10.2 先加上登入保護，V10.3A 後升級為 `owner/admin` 權限保護。
- `GET /api/menu` 維持公開。
- `POST /api/menu`、`PATCH /api/menu/:id`、`DELETE /api/menu/:id` 不再是公開操作。
- 新增版本歷史 API：`GET /api/menu/:id/history`。
- 版本歷史 API 可接受 logical id 或版本 id。
- 版本歷史會依版本由新到舊排序。
- 版本歷史會回傳 id、logical id、version、price、is current、supersedes、change reason、created at 等資料。
- 前端可顯示版本歷史。
- 編輯表單支援填寫修改原因。
- 修改原因可用於展示改價或調整原因。

### 圖片 UX 修正

使用者在 Render 線上驗證時發現：新增圖片 URL 後，畫面看起來仍使用固定 Unsplash 圖片。實際原因是前端圖片載入失敗時會靜默切換到固定 fallback 圖，導致使用者誤以為系統沒有儲存自己的 URL。

已完成修正：

- 新增圖片載入狀態處理。
- 不再靜默切換成固定 fallback。
- 圖片載入失敗時明確顯示「圖片載入失敗，顯示備用圖」。
- 顯示「開啟原圖」連結，方便檢查原始 URL。
- 菜單管理表單新增圖片 URL 即時預覽。
- 預覽失敗時提示「請使用可公開存取的圖片直連網址」。
- 圖片 URL 驗證維持寬鬆，不限制副檔名。
- 支援 `https://...`。
- 支援站內相對路徑，例如 `/imgs/menu/example.webp`。
- 後端資料儲存邏輯不需修改。

### 已驗證結果

- 使用者已在 Render 線上驗證菜單管理功能成功。
- 登入後可新增菜單。
- 登入後可編輯菜單並建立新版。
- 登入後可下架菜單。
- 可查看版本歷史。
- 圖片有效 URL 可正常顯示使用者輸入的圖片。
- 圖片無效 URL 會顯示錯誤提示、備用圖標示與「開啟原圖」連結。
- `/api/menu` 的 `image_url` 仍保留使用者輸入的原始值。

## V10.3A RBAC 基礎切片

### 目標

V10.3A 的目標是先建立最小可用的角色權限系統，讓菜單管理不再只是「登入即可操作」，而是由 `owner/admin` 才能管理。這一輪刻意不做完整角色申請與審核後台，以降低 migration 和 Render 部署風險。

### 已完成任務

- `bf_v10.user` 新增 `roles text[]` 欄位。
- roles 預設為 `["customer"]`。
- 新增 idempotent migration：`drizzle-v10/0001_v10_rbac_roles.sql`。
- migration 已加入 `drizzle-v10/meta/_journal.json`，Render 啟動 migration 時會套用。
- shared contract 新增固定角色：`customer`、`staff`、`chef`、`owner`、`admin`。
- `SessionUser` 新增 `roles`。
- 新增 `shared/guards.ts`：`hasRole`、`hasAnyRole`、`hasAllRoles`、`requireAnyRole`。
- 新增 `GET /api/users/me`。
- 前端登入恢復改用 `/api/users/me`，取得 app-level user 與 roles。
- `RBAC_ADMIN_EMAILS` 可用逗號分隔 email，登入時自動把指定 email 補成 `admin`。
- `POST /api/menu`、`PATCH /api/menu/:id`、`DELETE /api/menu/:id` 改為只有 `owner/admin` 可用。
- `GET /api/orders` 改為只有 `staff/chef/owner/admin` 可查看所有訂單。
- 現有 `PATCH /api/orders/:id` 維持顧客更新購物車品項數量的語意。
- 前端 navbar 顯示目前角色。
- 前端只有 `owner/admin` 看得到菜單管理區。
- 一般 `customer` 仍可正常瀏覽菜單、加入購物車、送出訂單與查看自己的歷史訂單。
- 權限不足時，前端顯示「目前角色沒有管理菜單的權限」。

### 已驗證結果

- Render env 新增 `RBAC_ADMIN_EMAILS=<你的 Google email>`。
- 部署後 migration 成功套用 roles 欄位。
- 使用指定 email 登入後，navbar 顯示管理員角色。
- admin 可新增、編輯、下架菜單。
- 非 admin/owner 登入後看不到菜單管理區。
- customer 仍可下單與查看自己的訂單。
- 線上 `/api/users/me` 回傳 roles。
- 使用者已回報 Render 完全驗證成功。

## V10.3B 角色申請與 Admin 審核

### 目標

V10.3B 的目標是補上 RBAC 後半段的角色升級流程。一般使用者可以送出 `staff/chef` 申請，`admin` 可以查看待審核申請並核准或拒絕。

### 已完成任務

- 新增 `role_requests` table。
- 新增 idempotent migration：`drizzle-v10/0002_v10_role_requests.sql`。
- migration 已加入 `drizzle-v10/meta/_journal.json`，Render 啟動 migration 時會套用。
- 新增角色申請 contracts 與 route schemas。
- `requestedRole` 僅允許 `staff` 或 `chef`。
- `status` 僅允許 `pending`、`approved`、`rejected`。
- 同一使用者同時間只能有一筆 `pending` 申請。
- 已擁有的角色不能重複申請。
- 新增 `POST /api/users/me/role-request`。
- 新增 `GET /api/users/me/role-requests`。
- 新增 `GET /api/admin/role-requests`。
- 新增 `PATCH /api/admin/role-requests/:id`。
- admin 核准申請後，系統會把 requested role 合併進目標使用者 roles，保留 `customer` 並去重。
- 前端同頁新增「角色申請」卡片。
- admin 額外看到「角色申請審核」卡片。

### 待 Render 驗證

- Render migration log 出現 `0002_v10_role_requests`。
- 一般帳號可送出 `staff/chef` 申請。
- 重複 pending 申請會被阻擋。
- admin 帳號可看到角色申請審核區。
- admin 核准後，一般帳號重新整理或重新登入後 navbar 顯示新角色。
- admin 拒絕後，一般帳號可看到 rejected 狀態且 roles 不變。

## 已完成驗證清單

### 本機驗證

- `bun test` 通過。
- `bun run build` 通過。
- 入口檔 TypeScript check 通過。
- `git diff --check` 無實質 whitespace error；Windows 環境只出現 LF/CRLF 提醒。

使用過的 TypeScript check：

```bash
bunx tsc --noEmit --skipLibCheck --moduleResolution bundler --module esnext --target esnext --jsx react-jsx --allowImportingTsExtensions backend.ts frontend/src/App.tsx tests/v10-menu-versioning.test.ts tests/v10-rbac.test.ts tests/v10-role-requests.test.ts
```

### Render 線上驗證

- Render build 成功。
- Render runtime 成功。
- migration 啟動流程成功。
- Neon `bf_v10` schema 可正常使用。
- 網站可登入。
- 菜單可讀取。
- 菜單可新增。
- 菜單可編輯並建立新版。
- 菜單可下架。
- 版本歷史可查詢。
- 圖片預覽與錯誤提示可正常運作。

## 重要 API 清單

### 菜單

- `GET /api/menu`
  - 公開 API。
  - 回傳目前 current menu items。

- `POST /api/menu`
  - 需要 `owner/admin`。
  - 新增菜單品項初版。

- `PATCH /api/menu/:id`
  - 需要 `owner/admin`。
  - 建立菜單新版。
  - `id` 可使用 logical id 或版本 id。

- `DELETE /api/menu/:id`
  - 需要 `owner/admin`。
  - 下架目前版本。
  - 不刪除歷史版本。

- `GET /api/menu/:id/history`
  - 查詢同一品項所有版本。
  - `id` 可使用 logical id 或版本 id。

### 使用者與權限

- `GET /api/users/me`
  - 需要登入。
  - 回傳目前登入使用者與 roles。

- `POST /api/users/me/role-request`
  - 需要登入。
  - 申請 `staff` 或 `chef`。

- `GET /api/users/me/role-requests`
  - 需要登入。
  - 回傳自己的角色申請紀錄。

- `GET /api/admin/role-requests`
  - 需要 `admin`。
  - 回傳角色申請列表，可依 status 篩選。

- `PATCH /api/admin/role-requests/:id`
  - 需要 `admin`。
  - 核准或拒絕 pending 角色申請。

### 訂單

- 訂單相關 API 維持需要登入。
- `GET /api/orders` 需要 `staff/chef/owner/admin`，用於查看所有訂單。
- 加入購物車只允許 current menu version。
- 送出訂單時若購物車內品項已過期，會回 `409`。

## 重要檔案清單

### 後端

- `backend.ts`
- `store/Store.ts`
- `store/pg/PgStore.ts`
- `store/json/JsonFileStore.ts`
- `db/repositories/menuRepository.ts`
- `shared/contracts.ts`
- `shared/route-schemas.ts`
- `shared/guards.ts`

### 資料庫

- `db/schema.ts`
- `db/auth-schema.ts`
- `db/schema-name.ts`
- `drizzle.config.ts`
- `drizzle-v10/0000_v10_initial.sql`
- `drizzle-v10/0001_v10_rbac_roles.sql`
- `drizzle-v10/0002_v10_role_requests.sql`
- `scripts/run-migration.ts`
- `scripts/start.ts`

### 前端

- `frontend/src/App.tsx`
- `frontend/src/index.css`

### 測試與文件

- `tests/v10-menu-versioning.test.ts`
- `tests/v10-rbac.test.ts`
- `tests/v10-role-requests.test.ts`
- `AGENTS.md`
- `報告.md`

## 未來預計任務

以下項目尚未實作，若繼續推進，建議歸類為 `V10.3C` 或後續版本。

### V10.3C 權限與管理

- admin 直接分配或移除使用者角色。
- 角色管理前端 UI。
- owner/admin 自助申請或升級流程。
- 管理操作審計紀錄。

### V10.3 菜單進階功能

- 菜單顯示排序 display order。
- 拖曳排序 UI。
- major/minor version。
- 更明確的版本命名規則。
- 菜單版本比較畫面。
- 批次上架或下架。

### 後續商業功能

- 促銷系統。
- 折扣碼。
- 套餐或加購項目。
- A/B testing。
- 熱門品項統計。
- 管理者 dashboard。

### UX 強化

- 購物車內品項過期時，自動提示可替換為新版。
- 菜單改價時更清楚標示新舊價格。
- 圖片 URL 輸入時提供更完整的格式提示。
- 管理表單欄位驗證訊息更細緻。
- 操作成功或失敗 toast 訊息更完整。

### 測試強化

- 補 API integration smoke test。
- 補登入後菜單管理流程測試。
- 補版本歷史 API 測試。
- 補 stale cart 送出訂單的端到端測試。
- 補前端圖片失敗提示的元件測試。

## 專案目前狀態判定

截至目前，專案可視為：

- `V10.1`：完成。
- `V10.2`：完成。
- `V10.3A`：完成，已 Render 驗證。
- `V10.3B`：已本機實作，待 Render 驗證。

若下一個 Codex/session 接手，請不要再把 V10.1、V10.2 或 V10.3A 視為待完成項目。除非使用者回報新的線上錯誤，否則下一步應該先協助驗證 V10.3B 的 Render 結果，或規劃 V10.3C。
