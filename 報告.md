# 早餐店訂餐系統 V10 實作報告

## 摘要

本專案已完成 V10.1 到 V10.3B 的主要線上驗證，並已本機完成 V10.3C 與本輪 V10.3D。
V10.3D 的重點是 RBAC 審計紀錄：讓展示時可以清楚說明「誰在什麼時間，因為什麼操作，改了誰的角色」。

## 已完成並由使用者回報 Render 驗證成功

### V10.1 基礎版本化

- 菜單品項改成版本化資料模型。
- 訂單引用特定菜單版本。
- 舊版菜單不可再加入購物車。
- 舊購物車送出時回 `409`。
- 歷史訂單保留當時版本的品名與價格。

### V10.2 菜單管理與版本展示

- 登入後可新增、編輯、下架菜單。
- 新增 `GET /api/menu/:id/history`。
- 前端顯示 logical id、版本 badge、近期更新提示。
- 圖片 URL 預覽與載入失敗提示已修正。

### V10.3A RBAC 基礎

- `user.roles` 欄位完成。
- `RBAC_ADMIN_EMAILS` 可自動授予初始 admin。
- `GET /api/users/me` 回傳 roles。
- 菜單管理需要 `owner/admin`。
- 查看所有訂單需要 `staff/chef/owner/admin`。
- 前端依 roles 顯示或隱藏管理 UI。

### V10.3B 角色申請與 Admin 審核

- 新增 `role_requests` table。
- 一般使用者可申請 `staff/chef`。
- admin 可核准或拒絕申請。
- 核准後角色會合併進使用者 roles。
- 使用者已回報 admin、staff/chef 申請、核准與拒絕流程皆已透過 Render 驗證。

## 已本機完成，待 Render 驗證

### V10.3C Admin 使用者角色管理

- 新增 `GET /api/admin/users`。
- 新增 `PATCH /api/admin/users/:userId/roles`。
- admin 可直接調整使用者 roles。
- 後端永遠保留 `customer`。
- 禁止 admin 移除自己的 `admin` role。
- 前端新增「使用者角色管理」卡片。

### V10.3D RBAC 審計紀錄

本輪新增：

- 新增 `role_audit_logs` table。
- 新增 migration：`drizzle-v10/0003_v10_role_audit_logs.sql`。
- 新增 audit action：
  - `role_request_approved`
  - `role_request_rejected`
  - `admin_roles_updated`
- 新增 `GET /api/admin/role-audit-logs`。
- admin 核准角色申請時寫入審計紀錄。
- admin 拒絕角色申請時寫入審計紀錄。
- admin 直接更新使用者 roles 時寫入審計紀錄。
- 前端新增「角色異動紀錄」卡片。
- 支援 action filter：全部、申請核准、申請拒絕、直接更新。

不在本輪範圍：

- 不回填歷史審計紀錄。
- 不記錄登入、訂單、菜單異動。
- 不做分頁、搜尋、匯出。
- 不改既有權限規則。
- 不追溯 `RBAC_ADMIN_EMAILS` 自動授予 admin 的紀錄。

## V10.3D API 摘要

### `GET /api/admin/role-audit-logs`

- 需要 `admin`。
- 預設回傳最新 100 筆。
- optional query：
  - `targetUserId`
  - `actorUserId`
  - `action`
- 回傳欄位包含：
  - `id`
  - `actorUserId`
  - `targetUserId`
  - `action`
  - `oldRoles`
  - `newRoles`
  - `source`
  - `roleRequestId`
  - `note`
  - `createdAt`
  - `actorName`
  - `actorEmail`
  - `targetName`
  - `targetEmail`

## 本機驗證命令

```bash
bun test
bun run build
bunx tsc --noEmit --skipLibCheck --moduleResolution bundler --module esnext --target esnext --jsx react-jsx --allowImportingTsExtensions backend.ts frontend/src/App.tsx tests/v10-menu-versioning.test.ts tests/v10-rbac.test.ts tests/v10-role-requests.test.ts tests/v10-admin-users.test.ts tests/v10-role-audit-logs.test.ts
git diff --check
```

## Render 驗證清單

V10.3D 部署後請確認：

- Render migration log 出現 `0003_v10_role_audit_logs`。
- admin 登入後看到「角色異動紀錄」區塊。
- 核准一筆 staff/chef 申請後，審計紀錄出現「申請核准」。
- 拒絕一筆申請後，審計紀錄出現「申請拒絕」。
- 直接修改一般使用者 roles 後，審計紀錄出現「直接更新」。
- 審計紀錄顯示操作者、目標使用者、變更前 roles、變更後 roles、時間與備註。
- 非 admin 看不到此區塊，也無法呼叫 `/api/admin/role-audit-logs`。

## 後續候選任務

### RBAC 後續

- V10.3C/V10.3D Render 線上驗證結果回填文件。
- audit log 分頁。
- audit log 匯出。
- admin 使用者搜尋與分頁。
- owner/admin 角色交接流程。

### 營運流程

- 店員/廚房專用訂單工作台。
- 訂單狀態流程細分。
- 出餐狀態與通知。

### 菜單功能

- display order。
- major/minor version。
- 版本比較 UI。
- 促銷系統。
- A/B testing。

### 測試

- 加強 API integration smoke tests。
- 加強登入後權限切換測試。
- 加強 stale cart 與歷史訂單回歸測試。
